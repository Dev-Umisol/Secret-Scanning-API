# Secret-Scanning-API
A developer security tool that accepts a GitHub Repo URL, scans all files for accidental committed secrets: API keys, tokens, passwords, and returns a severity ranked report displayed via React dashboard 
